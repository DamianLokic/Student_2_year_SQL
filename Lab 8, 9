--SELECT E.FIRST_NAME, E.LAST_NAME, D.DEPARTMENT_NAME FROM EMPLOYEES E LEFT OUTER JOIN DEPARTMENTS D ON(E.DEPARTMENT_ID = D.DEPARTMENT_ID)
--SELECT E.FIRST_NAME, E.LAST_NAME, D.DEPARTMENT_NAME FROM EMPLOYEES E RIGHT OUTER JOIN DEPARTMENTS D ON(E.DEPARTMENT_ID = D.DEPARTMENT_ID)
--SELECT E.FIRST_NAME, E.LAST_NAME, D.DEPARTMENT_NAME FROM EMPLOYEES E FULL OUTER JOIN DEPARTMENTS D ON(E.DEPARTMENT_ID = D.DEPARTMENT_ID)
--SELECT FIRST_NAME, LAST_NAME, EVENT_DATE, DESCRIPTION FROM D_EVENTS D RIGHT OUTER JOIN D_CLIENTS C ON(D.CLIENT_NUMBER = C.CLIENT_NUMBER)
--SELECT E.FIRST_NAME, E.LAST_NAME, D.DEPARTMENT_NAME FROM EMPLOYEES E FULL OUTER JOIN DEPARTMENTS D ON(E.DEPARTMENT_ID = D.DEPARTMENT_ID) WHERE LAST_NAME IS NULL

--SELECT ROUND(AVG(COST)) FROM D_EVENTS 
--SELECT SUM(SALARY) FROM F_STAFFS WHERE ID IN (12,9)
--SELECT MIN(SALARY) FROM EMPLOYEES WHERE DEPARTMENT_ID IN (50,60)
--SELECT MAX(HIRE_DATE) FROM EMPLOYEES WHERE DEPARTMENT_ID IN (50,60)    
--SELECT MIN(LAST_NAME) FROM EMPLOYEES WHERE DEPARTMENT_ID IN (50,60)
--SELECT AVG(ORDER_TOTAL) FROM F_ORDERS WHERE ORDER_DATE BETWEEN '01-Jan-2001' AND '21-Dec-2002'

--SELECT COUNT(ID) FROM D_SONGS
--SELECT COUNT(DISTINCT LOC_TYPE) FROM D_VENUES
--SELECT COUNT(EMAIL) FROM D_CLIENTS  
--SELECT COUNT(AUTH_EXPENSE_AMT) FROM D_PARTNERS

--SELECT AVG(NVL(AUTH_EXPENSE_AMT,100000)) FROM D_PARTNERS

--SELECT MANAGER_ID FROM EMPLOYEES WHERE SALARY < 16000 GROUP BY MANAGER_ID

--SELECT CD_NUMBER FROM D_CDS WHERE CD_NUMBER < 93 

--SELECT ID, MAX(ID) FROM D_SONGS WHERE DURATION IN('3 min', '6 min', '10 min') GROUP BY ID HAVING ID < 50

--SELECT RENTAL_FEE FROM D_VENUES WHERE ID <100 GROUP BY RENTAL_FEE ORDER BY 1

--SELECT MIN(AVG(SALARY)),MAX(AVG(SALARY)) FROM EMPLOYEES GROUP BY DEPARTMENT_ID

--SELECT AVG(MAX(SALARY)) FROM EMPLOYEES GROUP BY DEPARTMENT_ID
